services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: nexia
      POSTGRES_USER: nexia
      POSTGRES_PASSWORD: nexia
    ports:
      - "5432:5432"
    volumes:
      - nexia_pgdata:/var/lib/postgresql/data

volumes:
  nexia_pgdata:
